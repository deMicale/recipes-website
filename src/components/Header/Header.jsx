import {HeaderWrapper, 
        TitleHeader} from "./Header.elements.js";

const Header = () =>{
    return(
        <HeaderWrapper>
            <TitleHeader>
                Bread, love and fantasy!
            </TitleHeader>
        </HeaderWrapper>
    )
}

export default Header;